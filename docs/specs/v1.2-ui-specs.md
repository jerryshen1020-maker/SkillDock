# SkillDock v1.2 UI 规范文档

> ⚠️ **约束文档**：所有 UI 实现必须严格遵循本规范
>
> 参考设计稿：`设计/v1.2-SkillDock/01-设计稿.html`
>
> **注意**：本规范采用纯文档约束方式，不在代码中引入 DesignTokens

---

## 1. 总体布局

### 1.1 窗口结构

```
┌─────────────────────────────────────────────────────────────┐
│  Title Bar (52px)                                           │
├──────────┬─────────────────────────────────────────────────┤
│          │  Content Header (variable)                       │
│  Sidebar │  ─────────────────────────────────────────────  │
│ (200px)  │  │                                             │
│          │  │  Skill Grid (320px min, auto-fill)          │
│          │  │                                             │
│          │  └─────────────────────────────────────────────┘
└──────────┴─────────────────────────────────────────────────┘
```

### 1.2 约束规则

- **侧边栏宽度**：固定 `200px`
- **窗口最小尺寸**：`900 x 600`
- **内容区背景**：`#fafbfc`

---

## 2. 侧边栏 (Sidebar)

### 2.1 结构

```
┌──────────────┐
│ [C] Claude  │  ← AppEntry (active)
│ [C] Codex   │  ← AppEntry
│ [O] OpenCode│  ← AppEntry
│ [T] Trae    │  ← AppEntry
│ [T] Trae CN │  ← AppEntry
│              │
│ ─────────    │
│ 🗂️ 来源      │  ← SidebarEntry (bottom)
│ ⚙️ 设置      │  ← SidebarEntry (bottom)
└──────────────┘
```

### 2.2 AppEntryView 组件规范

| 状态 | 背景 | 文字色 |
|------|------|--------|
| Default | `#f1f3f5` | `#868e96` |
| Hover | `#e9ecef` | `#212529` |
| Active | `#e7f5ff` | `#4c6ef5` |

**尺寸**：
- `padding`：10px horizontal, 10px vertical
- `iconSize`：20px（app-icon）
- `fontSize`：13px
- `cornerRadius`：10px
- `marginBottom`：2px

### 2.3 应用图标

每个应用有专属颜色 logo 图标：

| 应用 | 图标样式 | 背景色 |
|------|---------|--------|
| Claude | C | `#4c6ef5` |
| Codex | C | `#228be6` |
| OpenCode | O | `#12b886` |
| Trae | T | `#fab005` |
| Trae CN | T | `#fa5252` |

---

## 3. 顶部标题栏 (Titlebar)

### 3.1 结构

```
┌─────────────────────────────────────────────────────────────┐
│  🧠 SkillDock         [🔍 搜索 skills...]                  │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 约束规则

- **高度**：52px
- **背景**：`#ffffff`
- **边框底**：1px `#dee2e6`
- **左侧 Logo**：28x28px，圆角 8px
- **搜索框宽度**：280px

---

## 4. 内容区标题 (Content Header)

### 4.1 Skills 列表页面

```
┌─────────────────────────────────────────────────────────────┐
│  Claude - Skills            共 12 个         [↻ 一键同步]  │
└─────────────────────────────────────────────────────────────┘
```

- **高度**：自适应
- **背景**：`#ffffff`
- **边框底**：1px `#f1f3f5`
- **标题字号**：18px，字重 600
- **一键同步按钮**：主色 `#4c6ef5`，白字

---

## 5. 技能卡片 (SkillCardView)

### 5.1 卡片结构

```
┌─────────────────────────────────────┐
│ 🧠 brainstorming       [删除]       │
│ 来源: ~/.claude/skills              │
│                                     │
│ 帮助把想法变成完整的设计和规格...     │
└─────────────────────────────────────┘
```

### 5.2 尺寸规范

| 属性 | 值 |
|------|-----|
| minWidth | 320px |
| padding | 16px |
| cornerRadius | 14px |
| gap | 12px |

### 5.3 颜色规范

| 状态 | 背景 | 边框 | 阴影 |
|------|------|------|------|
| Default | `#ffffff` | `#f1f3f5` | - |
| Hover | `#ffffff` | `#4c6ef5` | `rgba(0,0,0,0.06)` |

### 5.4 文字规范

| 元素 | 大小 | 颜色 | 字重 |
|------|------|------|------|
| 名称 | 15px | `#212529` | 600 |
| 来源 | 11px | `#adb5bd` | regular |
| 描述 | 13px | `#868e96` | regular |

### 5.5 图标规范

| 元素 | 尺寸 | 背景 |
|------|------|------|
| 技能图标 | 36px | `#e7f5ff` |
| 圆角 | 10px | - |

---

## 6. 来源管理页面 (Source Page)

### 6.1 结构

```
┌─────────────────────────────────────────────────────────────┐
│  来源管理                                                   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  本地目录                                    [+ 添加目录]  │
│  ─────────────────────────────────────────────────────────  │
│  📁 ~/.claude/skills                            [删除]    │
│  📁 ~/Documents/MySkills                        [删除]    │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  Git 仓库                              [↻ 一键更新] [+ 添加仓库]│
│  ─────────────────────────────────────────────────────────  │
│  📦 farion1231/browser-use                       [删除]   │
│  📦 qufei1993/skills-hub                        [删除]   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 来源项样式

- **高度**：48px
- **内边距**：12px
- **背景**：`#ffffff`
- **边框**：1px `#f1f3f5`
- **圆角**：10px
- **删除按钮**：hover 时显示

### 6.3 按钮排列

- 本地目录：标题 + 按钮（右侧）
- Git 仓库：标题 + 按钮组（右侧，水平排列，gap: 8px）

---

## 7. 设置页面 (Settings Page)

### 7.1 结构

```
┌─────────────────────────────────────────────────────────────┐
│  设置                                                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  SkillDock v1.2.0                                         │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  应用主题                                                   │
│  ─────────────────────────────────────────────────────────  │
│  ○ 跟随系统                                                │
│  ○ 浅色                                                    │
│  ○ 深色                                                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 7.2 内容宽度

- **最大宽度**：400px
- **居中显示**：margin: 0 auto

---

## 8. 弹窗组件 (Modal)

### 8.1 添加本地目录弹窗

```
┌─────────────────────────────────────────┐
│  添加本地目录                     [×]    │
├─────────────────────────────────────────┤
│                                         │
│  请选择要添加的目录:                  │
│  ┌─────────────────────────────┐ [选择] │
│  │ ~/Documents/MySkills      │        │
│  └─────────────────────────────┘        │
│                                         │
│            [取消]      [添加]            │
└─────────────────────────────────────────┘
```

### 8.2 添加 Git 仓库弹窗

```
┌─────────────────────────────────────────┐
│  添加 Git 仓库                   [×]    │
├─────────────────────────────────────────┤
│                                         │
│  仓库地址:                            │
│  ┌─────────────────────────────┐        │
│  │ https://github.com/...   │        │
│  └─────────────────────────────┘        │
│                                         │
│  分支 (可选):                         │
│  ┌─────────────────────────────┐        │
│  │ main                       │        │
│  └─────────────────────────────┘        │
│                                         │
│            [取消]      [添加]            │
└─────────────────────────────────────────┘
```

### 8.3 弹窗约束

- **宽度**：480px
- **背景**：`#ffffff`
- **圆角**：14px
- **遮罩**：rgba(0,0,0,0.4)
- **关闭按钮**：右上角，hover 变色

---

## 9. Toast 组件

### 9.1 规范

| 类型 | 时长 | 颜色 | 最大数量 |
|------|------|------|----------|
| Success | 2.0s | `#40c057` | 3 |
| Warning | 3.0s | `#fab005` | 2 |
| Error | 5.0s | `#fa5252` | 1 |

### 9.2 布局

- 位置：右下角
- `padding`：24px
- `gap`：8px
- 动画：`slideIn` (从右侧滑入)

---

## 10. 空状态 (Empty States)

### 10.1 规范

| 场景 | 图标 | 主文案 |
|------|------|--------|
| 无 Skills | 🧩 | 该应用暂无 Skills |
| 搜索无结果 | 🔍 | 未找到匹配的 Skills |

**样式**：
- `iconSize`：48px
- `opacity`：0.5
- `padding`：48px

---

## 11. 交互行为规则表

| 触发 | 用户操作 | 系统行为 | 反馈 |
|------|----------|----------|------|
| 用户 | hover 应用列表 | 切换到对应应用的 Skills 列表 | 高亮当前应用 |
| 用户 | 点击"一键同步" | 同步当前应用的所有 Skills | Toast: 同步中 → 完成/失败 |
| 用户 | 输入搜索关键词 | 实时过滤 Skills | 无结果时显示空态 |
| 用户 | 点击 Skill"删除" | 从应用目录删除该 Skill | Toast: 已删除 Skill |
| 用户 | 点击"来源" | 切换到来源管理页面 | - |
| 用户 | 点击"+ 添加目录" | 打开目录选择器 | 添加成功后刷新列表 |
| 用户 | 点击"+ 添加仓库" | 打开 Git 添加弹窗 | 添加成功后刷新列表 |
| 用户 | 点击"↻ 一键更新" | 批量更新所有 Git 仓库 | Toast: 更新中 → 完成/失败 |
| 用户 | 点击来源"删除" | 删除该来源配置 | Toast: 已删除来源 |
| 用户 | 点击"设置" | 切换到设置页面 | - |
| 系统 | 启动时 | 加载配置，扫描所有来源目录 | 显示加载态 → 正常态/错误态 |
| 系统 | 检测到目录不存在 | 标记该来源为不可用 | Toast 警告 + 错误态 UI |
| 系统 | Git 同步失败 | 显示错误状态 | Toast Error + 错误态 UI |

---

## 12. 验收检查清单

### 12.1 侧边栏

- [ ] 宽度固定 200px
- [ ] 应用项圆角 10px
- [ ] Active 状态背景为 `#e7f5ff`
- [ ] 应用图标显示正确颜色

### 12.2 技能卡片

- [ ] 最小宽度 320px
- [ ] 圆角 14px
- [ ] Hover 时边框变为 `#4c6ef5`
- [ ] 图标尺寸 36x36，背景 `#e7f5ff`

### 12.3 来源管理

- [ ] 来源项显示图标和路径
- [ ] 删除按钮 hover 时显示
- [ ] Git 仓库按钮水平排列对齐

### 12.4 设置页面

- [ ] 内容宽度 400px 居中
- [ ] 主题选项正确显示

### 12.5 弹窗

- [ ] 宽度 480px
- [ ] 遮罩显示
- [ ] 关闭按钮可用

### 12.6 Toast

- [ ] 右下角显示
- [ ] Success 2s、Warning 3s、Error 5s

### 12.7 颜色

- [ ] 主色 `#4c6ef5`
- [ ] 背景主 `#fafbfc`

---

## 13. 常见错误 ⚠️

| 错误 | 正确做法 |
|------|----------|
| 硬编码颜色值 | 使用本规范文档中定义的颜色值 |
| 随意圆角 | 使用本规范文档中定义的圆角值 |
| 随意间距 | 使用本规范文档中定义的间距值 |
| 忽略 Hover 态 | 所有可交互元素需定义 Hover 态 |
| 缺少状态设计 | 确保覆盖正常/加载/空/错误四种状态 |

---

*创建时间: 2026-03-04*
*版本: v1.2*
*适用范围: 所有 v1.2 UI 实现*
*约束方式: 纯文档约束（无 DesignTokens 代码）*
